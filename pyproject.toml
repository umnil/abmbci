[build-system]
requires = ["setuptools", "wheel", "pybind11", "scikit-build"]
build-backend = "setuptools.build_meta"

[project]
name = "abmbci"
version = "0.1.25"
dependencies = [
    "numpy",
    "pandas",
    "pyserial"
]